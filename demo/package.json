{
  "name": "torollo-demo-recording",
  "private": true,
  "type": "module",
  "description": "Records the README/website product demo against a real Torollo backend and real Docker containers.",
  "scripts": {
    "setup": "node setup.mjs",
    "record": "node record.mjs",
    "encode": "bash encode.sh",
    "all": "node setup.mjs && node record.mjs && bash encode.sh"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright-core": "^1.56.0"
  }
}
